setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: frontend/Dockerfile
    api: backend/Dockerfile
run:
  web: npx serve -s /user/src/app/build
  api: npm run start:prod